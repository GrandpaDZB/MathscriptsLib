clc;clear
disp("======== Mode ========");
disp("1 Linear Regression");
disp("2 Polynomial Curve Fitting");
disp("3 DIY");
disp("=======================");

mode = input("Mode = ");

switch (mode)
   
    case 1
        y = input("(y1,...,yn)' y = ");
        X = input("(x1',...,xn') X = ");
        w = (X'*X)\X'*y;
        disp(w);
        
    case 2
        y = input("(y1,...,yn)' y = ");
        x = input("(x1,...,xn)' x = ");
        n = input("dim = ");
        X = ones(length(x),dim+1);
        for i=1:dim
            X()
        end
        
    
    
end